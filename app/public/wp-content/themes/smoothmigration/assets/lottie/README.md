This folder stores self-hosted Lottie JSON animations used by the theme.

Expected filenames referenced in templates:
- hero-world.json
- realtor-accent.json

Notes:
- Files should be optimized (minified) to keep page weight low.
- Do not rename filenames without updating references in templates.

